name: BooksDotnetBuild
on:
  push:
    branches:
      - "master"
  pull-request:
    branches:
      - "master"

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3

      